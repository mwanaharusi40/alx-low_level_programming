#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
    int a;
    int b;

    a = 98;
    b = 42;
    
    _putchar("a,b");

    swap_int(&a, &b);
    _putchar("a, b":\n);

    return (0);
}
