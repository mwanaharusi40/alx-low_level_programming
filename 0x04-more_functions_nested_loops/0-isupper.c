#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _isupper(int c)
{
    char c;
   
   if (c = A)
    _putchar("A");

   else (c = a)
    _putchar("a");

    return (0);
}
