#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _isdigit(int c);
{
    char c;

   if (c = 0)
    _putchar("0");

   else (c = a)
    _putchar("a");

    return (0);
}
